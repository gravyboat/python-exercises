#!/usr/bin/py


def max_val(val_one, val_two):
    '''
    Takes two values and returns the largest of them
    '''
    if val_one > val_two:
        return(val_one)
    else:
        return(val_two)

