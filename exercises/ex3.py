#!/usr/bin/python

def str_length(string_to_count):
    '''
    Returns the lenght of a string
    '''

    total_length = 0

    for character in string_to_count:
        total_length += 1

    return(total_length)

